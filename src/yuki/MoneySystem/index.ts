import { transact } from "./TransactionBuilder"
import { walletCache, startingWallet, name } from "./config"
import leaderboard from "./leaderboard"

export default { walletCache, transact, name, startingWallet, leaderboard }
